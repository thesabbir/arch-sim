Do not edit generated-\* files unless you are sure you need to.

### Runtime

Node.js

### Package Manager

Use pnpm for package management. And run commands with pnpm.

### Commands

Use the following commands to run the project:

```
pnpm start
```

### Test Runner

Use the following commands to run the test runner:

```
pnpm test
```

### Simulator

Use the following commands to run the simulator:

```
pnpm simulate
```

### Generate Platform Ecosystem

Use the following commands to generate the platform ecosystem:

```
pnpm generate
```
